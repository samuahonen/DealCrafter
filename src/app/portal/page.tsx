import { ClientPortal } from "@/components/ClientPortal";

export const metadata = {
  title: "Proposal — DealCraft",
  description: "Review and accept your proposal",
};

export default function PortalPage() {
  return <ClientPortal />;
}
